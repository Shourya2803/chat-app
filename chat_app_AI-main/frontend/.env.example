# Frontend environment variables example for local development
# DO NOT commit real secrets to the repo. Use Vercel Environment Variables for production.

# Public URLs (exposed to browser)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# Database (server-only)
# Set this in Vercel Dashboard (Environment Variables) for Production/Preview
DATABASE_URL=postgresql://<DB_USER>:<DB_PASS>@<DB_HOST>:5432/<DB_NAME>?sslmode=require

# Redis (server-only)
REDIS_URL=rediss://<USER>:<PASSWORD>@<HOST>:<PORT>

# Clerk (auth)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_XXXXXXXXXXXXXXXXXXXXXXXX
CLERK_SECRET_KEY=sk_XXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat

# Gemini AI API key (server-side secret)
GEMINI_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXX

# Firebase (frontend-only public keys)
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXX
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-firebase-app.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-firebase-app.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=1234567890
NEXT_PUBLIC_FIREBASE_APP_ID=1:1234567890:web:abcdef123456
NEXT_PUBLIC_FIREBASE_VAPID_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Optional: Prisma Data Proxy (recommended for serverless on Vercel)
# If using Data Proxy, set PRISMA_CLIENT_ENGINE_TYPE=dataproxy and set the following URL
# PRISMA_DATA_PROXY_URL=https://client.prisma.sh/your-org/your-project

# Notes:
# - For Vercel, add these values in Project Settings → Environment Variables.
#   * Add `DATABASE_URL`, `REDIS_URL`, `CLERK_SECRET_KEY`, `GEMINI_API_KEY`, etc. as server-side (do NOT prefix with NEXT_PUBLIC).
#   * Add `NEXT_PUBLIC_*` variables as public values so the browser can read them.
# - Never store real secret values in the repository. Use Vercel's web UI or CLI to set them.
